import com.lexer.Lexer;
import com.lexer.Token;


public class myclass extends biggerClass implements functions{

    public static int count = 0;
    private final float pi = 3.14;

    public float parse(Parser par){
        return 3.55;
    }

    public int sum(){
        return 0;
    }

    public void function(float u, int m){
        float k = 1.3;
        char gdf = 'f';
        u = 4.4;
        int stam = 13;
        int yo = 0;
        int y = function2(count,stam);
        //myclass x = new myclass(x,Y);
        int X = (y + 10) - 20;
        while(x < 20){
            y = (x == 10)? 5 : 8;
        }
    }

    public int function2(int x = 0, int y){
        if( y == 10){
         x = 10;
        } else{
            boolean b = true;
        }
        function(9.7,x);

        x = ++y;
        return 0 + 6;
    }
}

interface functions extends ha, yes{
    public float parse(Parser par);
    public int sum();
}


